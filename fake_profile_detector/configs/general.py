import os

TEST_SIZE = 0.2
VAL_SIZE = 0.3
RANDOM_SEED = 1337
BASE_DIR = base_dir = os.path.join(
    os.environ["HOME"],
    "master",
    "src",
    "fake_profile_detector",
    "datasets",
)
SAVE_DIR = base_dir = os.path.join(
    os.environ["HOME"],
    "master",
    "src",
    "fake_profile_detector",
    "results",
)
